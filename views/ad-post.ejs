<!DOCTYPE html>
<html>

<head>
  <meta charset="utf-8">
  <link rel="stylesheet" href="css/ad-post.css">
  <title></title>
</head>
<header>
  <nav>
    <form class="" action="/" method="post">
      <input class="locationSearch" type="text" placeholder="Search location" name="" value="">
      <button class="locationSearch" type="submit" name="button">Search</button>
    </form>
    <ul id="navList">
      <li><a class="about" href="/about">About</a></li>
      <li><a class="report" href="#">Report |</a></li>
      <li><a class="contact" href="/">Log-Out |</a></li>
    </ul>
  </nav>
</header>

<body>
  <section id="postContainer">
    <section id="topSection">
      <button>reply</button>
      <a href="#">
        < prev</a>
          <a href="#"> next ></a>
          <a href="#">report</a>
    </section>
    <section id="postContainer">
      <section id="details">
        <h1><%= message[i].adTitle %></h1>
        <h4><%= message[i].zip %></h4>

        <p>
          <%= message[i].textArea %>
        </p>

        <span> ft <sup>2: </sup><%= message[i].sqFeet %></span>
        <span>$ <%= message[i].minimum %></span>
        <span>$ <%= message[i].maximum %></span>
        <span>pets: <%= message[i].pets %></span>
        <span>family size: <%= message[i].family %></span>
        <span>email: <%= message[i].email %></span>
        <span>phone #: <%= message[i].phone %></span>
        <span> <%= ad.date.toDateString() %> </span>
      </section>
      <% }%>
        <% }%>

    </section>
  </section>
</body>

</html>